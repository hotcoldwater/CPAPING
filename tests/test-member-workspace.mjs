import test from 'node:test';import assert from 'node:assert/strict';import {readFileSync} from 'node:fs';import {createRequire} from 'node:module';
import {validateMember,onRequest as profile} from '../functions/api/me/profile.js';import {onRequest as career} from '../functions/api/career/[[path]].js';import {renderPostingPage} from '../web/posting-page.mjs';
const require=createRequire('/tmp/cpaping-career-qa/package.json'),{JSDOM}=require('jsdom'),{PGlite}=require('@electric-sql/pglite');
const uid='11111111-1111-4111-8111-111111111111',other='22222222-2222-4222-8222-222222222222';
const env={CAREER_ENABLED:'true',CAREER_RESUME_ONLY:'true',CAREER_RESUME_ENABLED:'true',SUPABASE_URL:'https://example.supabase.co',SUPABASE_SECRET_KEY:'fake'};
const json=x=>new Response(JSON.stringify(x),{headers:{'Content-Type':'application/json'}}),pause=()=>new Promise(r=>setTimeout(r,50));
const valid={nickname:null,full_name:'테스트 이름',birth_date:'1999-02-28',phone:'010-1234-5678',school:'예시대학교',pass_year:2020,research_consent:false};
test('optional profile accepts blanks but rejects impossible dates, years, invalid types and reserved names',()=>{
 assert.equal(validateMember({research_consent:false}).nickname,null);assert.deepEqual(validateMember(valid),valid);
 for(const change of [{birth_date:'2001-02-29'},{birth_date:'2999-01-01'},{phone:'<script>'},{pass_year:'2020'},{pass_year:2999},{research_consent:'true'},{nickname:'공식운영자'},{nickname:'a'},{school:'x'.repeat(121)}])assert.throws(()=>validateMember({...valid,...change}));
});
test('profile authenticates before reads and uses server owner for atomic save, with no public cache',async t=>{
 const calls=[];t.mock.method(globalThis,'fetch',async(url,opt)=>{calls.push({url:String(url),opt});return String(url).includes('/auth/')?json({id:uid,email_confirmed_at:'yes'}):json(null);});
 const request=new Request('https://cpaping.com/api/me/profile',{method:'PUT',headers:{Authorization:'Bearer test','Content-Type':'application/json'},body:JSON.stringify({...valid,user_id:other})});
 const r=await profile({request,env});assert.equal(r.status,200);assert.match(r.headers.get('Cache-Control'),/no-store/);assert.equal(JSON.parse(calls[1].opt.body).p_user,uid);assert.match(calls[1].url,/rpc\/save_member_details/);
 const anon=await profile({request:new Request('https://cpaping.com/api/me/profile'),env});assert.equal(anon.status,401);assert.equal(calls.length,2);
});
test('reply edit is owner-scoped, restricted to sent deliveries, and GET never approves a message',async t=>{
 const calls=[];t.mock.method(globalThis,'fetch',async(url,opt)=>{calls.push({url:String(url),opt});return String(url).includes('/auth/')?json({id:uid,email_confirmed_at:'yes'}):json([]);});
 const path=['deliveries',other,'reply'];const request=new Request('https://cpaping.com/api/career/'+path.join('/'),{method:'PUT',headers:{Authorization:'Bearer test','Content-Type':'application/json'},body:JSON.stringify({reply_status:'received',user_id:other})});
 const r=await career({request,env,params:{path}});assert.equal(r.status,404);assert.match(calls[1].url,new RegExp('user_id=eq.'+uid));assert.match(calls[1].url,/status=in\.\(sent,delivery_unknown\)/);assert.equal(JSON.parse(calls[1].opt.body).reply_status,'received');
 const get=await career({request:new Request('https://cpaping.com/api/career/resume-applications/'+uid,{headers:{Authorization:'Bearer test'}}),env,params:{path:['resume-applications',uid]}});assert.equal(get.status,404);
});
test('private member data and outbox deny public access; nickname and details save atomically; withdrawal clears consent timestamp',async()=>{
 const db=new PGlite();try{await db.exec(`create role anon;create role authenticated;create role service_role;create schema auth;create table auth.users(id uuid primary key);create table profiles(user_id uuid primary key references auth.users on delete cascade,nickname text unique,updated_at timestamptz);create table job_postings(id bigint primary key,removed_at timestamptz,is_expired boolean);insert into auth.users values('${uid}'),('${other}');`);
 for(const f of ['014_career_workspace.sql','016_mail_delivery_history.sql','017_uploaded_resume.sql','019_member_workspace.sql'])await db.exec(readFileSync('db/migrations/'+f,'utf8'));
 for(const role of ['anon','authenticated'])for(const table of ['member_details','career_review_notices'])assert.equal((await db.query('select has_table_privilege($1,$2,\'SELECT\') as ok',[role,table])).rows[0].ok,false);
 assert.equal((await db.query("select has_function_privilege('authenticated','save_member_details(uuid,text,text,date,text,text,integer,boolean)','EXECUTE') as ok")).rows[0].ok,false);
 const save=(id,nick,consent)=>db.query('select save_member_details($1,$2,$3,$4,$5,$6,$7,$8)',[id,nick,'테스트','1999-01-01',null,'예시대',2020,consent]);
 await save(uid,'하나',true);const first=(await db.query('select * from member_details')).rows[0];assert.ok(first.research_consented_at);await save(other,'둘',false);
 await assert.rejects(()=>save(other,'하나',true),/unique/);assert.equal((await db.query('select research_consent from member_details where user_id=$1',[other])).rows[0].research_consent,false);
 await save(uid,null,false);assert.equal((await db.query('select research_consented_at from member_details where user_id=$1',[uid])).rows[0].research_consented_at,null);
 await db.query('delete from auth.users where id=$1',[uid]);assert.equal((await db.query('select count(*)::int n from member_details where user_id=$1',[uid])).rows[0].n,0);
 }finally{await db.close();}
});
function auth(w){w.cpAuth={ensure:async()=>({state:'needs_profile',user:{id:uid,email:'self@example.com',created_at:'2026-01-01'},profile:{nickname:null}}),maskEmail:v=>v,client:{auth:{getSession:async()=>({data:{session:{access_token:'test'}}})},from:()=>({select:()=>({order:()=>({limit:async()=>({data:[{id:1,name:'예시법인'}]})})})})}};}
test('resume is separate from history, drag-drop selects actual bytes and preview replaces firm in both fields',async()=>{
 const dom=new JSDOM(readFileSync('web/resume.html','utf8'),{url:'https://cpaping.com/resume/',runScripts:'outside-only'}),w=dom.window;try{auth(w);const calls=[];w.fetch=async(url,opt)=>{calls.push({url,opt});return {ok:true,json:async()=>String(url).endsWith('/state')?{mail:null}:String(url).endsWith('/resumes')?{id:uid}:{files:[{id:uid,name:'resume.pdf'}],rule:null,applications:[],limit:100}};};
 w.eval(readFileSync('web/resume-preview.js','utf8'));w.eval(readFileSync('web/resume.js','utf8'));await pause();const d=w.document;assert.equal(d.getElementById('deliveries'),null);assert.equal(d.getElementById('resume-mode').value,'review');
 d.getElementById('applicant-name').value='테스트';d.getElementById('resume-subject').value='[회계법인] {이름}';d.getElementById('resume-body').value='[회계법인] 지원';d.getElementById('resume-form').dispatchEvent(new w.Event('input'));assert.equal(d.getElementById('preview-subject').textContent,'예시회계법인 테스트');assert.equal(d.getElementById('preview-body').textContent,'예시회계법인 지원');
 const file=new w.File(['%PDF-test'],'drop.pdf',{type:'application/pdf'}),drop=new w.Event('drop',{bubbles:true,cancelable:true});Object.defineProperty(drop,'dataTransfer',{value:{files:[file],types:['Files']}});d.getElementById('resume-drop').dispatchEvent(drop);assert.equal(w.cpResumeFile,file);d.getElementById('upload-resume').click();await pause();assert.equal(JSON.parse(calls.find(c=>c.url.endsWith('/resumes')).opt.body).data_base64,Buffer.from('%PDF-test').toString('base64'));
 }finally{dom.window.close();}
});
test('approval deep link opens only its matching review card and cannot send until checked and clicked',async()=>{
 const dom=new JSDOM(readFileSync('web/applications.html','utf8'),{url:'https://cpaping.com/applications/?review='+uid,runScripts:'outside-only'}),w=dom.window;try{auth(w);const calls=[];w.fetch=async(url,opt)=>{calls.push({url,opt});return {ok:true,json:async()=>({files:[],applications:[{id:uid,status:'review',version:1,document_id:other,snapshot:{company:'예시법인',allowed_recipients:['hr@example.com']},subject:'지원',body:'본문'}],limit:100})};};w.eval(readFileSync('web/resume.js','utf8'));await pause();const d=w.document;assert.equal(d.getElementById('resume-form'),null);const card=d.getElementById('application-'+uid);assert.equal(card.open,true);assert.ok(calls.every(c=>c.opt.method==='GET'));const send=[...card.querySelectorAll('button')].find(b=>b.textContent.includes('검수 완료'));send.click();await pause();assert.ok(calls.every(c=>c.opt.method==='GET'));card.querySelector('input[type=checkbox]').checked=true;send.click();await pause();assert.equal(calls.filter(c=>c.opt.method==='PUT').length,1);
 }finally{dom.window.close();}
});
test('posting time uses observed timestamp in Korea and never changes source publication date',()=>{const html=renderPostingPage({posting:{id:1,ij_id:'123',source:'kicpa:trainee',company_name:'예시',title:'채용',posted_at:'2026-09-13',first_seen_at:'2026-09-13T16:05:00Z'}});assert.match(html,/최초 확인 시간/);assert.match(html,/2026\. 9\. 14\. 01:05/);assert.match(html,/원문 등록 시각과 다를 수 있음/);});
test('account accepts a member without nickname, saves optional fields and updates the menu event',async()=>{
 const dom=new JSDOM(readFileSync('web/account.html','utf8'),{url:'https://cpaping.com/account/',runScripts:'outside-only'}),w=dom.window;try{auth(w);let saved,updated=false;w.addEventListener('cpaping:profile-updated',()=>updated=true);w.fetch=async(url,opt)=>({ok:true,json:async()=>opt.method==='PUT'?(saved=JSON.parse(opt.body),{ok:true,...saved}):{...valid,nickname:null}});const script=[...w.document.scripts].find(s=>!s.src);w.eval(script.textContent);await pause();const d=w.document;assert.equal(d.getElementById('profile-fields').disabled,false);assert.equal(d.getElementById('research-consent').checked,false);d.getElementById('nickname').value='테스트회원';d.getElementById('phone').value='';d.getElementById('profile-form').dispatchEvent(new w.Event('submit',{bubbles:true,cancelable:true}));await pause();assert.equal(saved.nickname,'테스트회원');assert.equal(saved.phone,null);assert.equal(saved.research_consent,false);assert.equal(updated,true);assert.equal(d.getElementById('nick').textContent,'테스트회원');
 }finally{dom.window.close();}
});
test('notification filters load separately and save without changing resume automation',async()=>{
 const dom=new JSDOM(readFileSync('web/notifications.html','utf8'),{url:'https://cpaping.com/notifications/',runScripts:'outside-only'}),w=dom.window;try{auth(w);let saved;w.fetch=async(url,opt)=>{assert.equal(url,'/api/me/subscription');if(opt.method==='PUT')saved=JSON.parse(opt.body);return {ok:true,json:async()=>({active:true,region_filter:'capital',want_trainee_full:true,want_trainee_part:false,want_career:true})};};w.eval([...w.document.scripts].find(s=>!s.src).textContent);await pause();const d=w.document;assert.equal(d.getElementById('region').value,'capital');d.getElementById('region').value='local';d.getElementById('alert-save').click();await pause();assert.equal(saved.region_filter,'local');assert.equal(saved.want_career,true);
 }finally{dom.window.close();}
});
test('nickname hydration uses safe text and clears the label on logout',async()=>{
 const {navigation}=await import('../web/navigation.mjs');const dom=new JSDOM(navigation('<head></head><header class="topbar"></header>'),{url:'https://cpaping.com/',runScripts:'outside-only'}),w=dom.window;try{w.localStorage.setItem('sb-test-auth-token',JSON.stringify({access_token:'fake'}));w.fetch=async()=>({ok:true,json:async()=>({nickname:'<img src=x>'})});w.eval(readFileSync('web/navigation.js','utf8'));await pause();assert.equal(w.document.getElementById('account-nickname').textContent,'<img src=x>');assert.equal(w.document.querySelectorAll('#account-menu img').length,0);w.localStorage.clear();w.dispatchEvent(new w.StorageEvent('storage'));assert.equal(w.document.getElementById('account-menu').hidden,true);assert.equal(w.document.getElementById('account-nickname').textContent,'내 계정');}finally{dom.window.close();}
});
