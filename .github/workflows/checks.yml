name: 코드 검증

on:
  pull_request:
  push:
    paths:
      - "crawler/**"
      - "web/**"
      - "functions/**"
      - "tests/**"
      - "db/**"
      - "career/**"
      - ".github/workflows/checks.yml"

permissions:
  contents: read

jobs:
  checks:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: |
            crawler/requirements.txt
            crawler/requirements-test.txt
      - run: pip install -r crawler/requirements-test.txt -r career/requirements.txt
      - run: python -m unittest discover -s tests -p 'test_*.py'
      - run: python -m unittest discover -s crawler -p 'test_*.py'
      # 기존 독립 실행 테스트는 unittest discover 에 잡히지 않는다.
      - run: python crawler/test_limits.py
      - uses: actions/setup-node@v4
        with:
          node-version: "24"
      - run: npm install --prefix /tmp/cpaping-career-qa @electric-sql/pglite@0.5.8 jsdom@30.0.1
      - run: node --test web/test-posting-page.mjs tests/test-mail-connection.mjs tests/test-delivery-history.mjs
      - run: node --check web/build.mjs
